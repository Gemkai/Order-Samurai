DAILY_BUDGET_USD=5.00
MAX_CONSECUTIVE_FAILS=5
ENABLED_RONINS="bow,sword,brush,arts"
VALIDATE_CMD="python execution/doctor.py"
RUN_HOURS=6
MAX_CYCLES=60
MAX_TURNS=80           # 4 parallel ronins × ~15 turns + Sensei orchestration overhead
CYCLE_TIMEOUT=2400         # 4 parallel agents need ~40 min; was 20 min for single agent
CYCLE_COST_USD=0.08        # 4 ronins × $0.02 each
COOLDOWN=15
LMSTUDIO_BASE="http://localhost:1234/v1"
RONIN_LOCAL_MODEL="qwen3-coder"
